# N.O.M.A.D. 8e.7.1 cache 957

Consolidated backlog and defect-correction release.

- Camera View and camera-source Video Wall renderers now use sensor-aspect letterboxing. Sidebar collapse, viewport resizing, and preset-panel resizing cannot stretch or reframe the camera image; ROI SVGs align to the fitted canvas.
- ROI overlays belong only to the actively recalled PTZ preset. Manual camera movement, pan, tilt, roll, zoom, or depth changes immediately deactivate the preset and hide its ROIs; recalling the preset restores them after the transition completes.
- Blank PTZ preset names increment per camera (`Preset 001`, `Preset 002`, etc.), while deliberate custom names remain supported.
- PTZ depth/ROI workflow status is isolated from Measurement Tools and no longer leaves ROI analysis in that dropdown.
- Configured camera-model changes are guarded by an original N.O.M.A.D. dialog. The user may cancel, replace and clear dependent configuration, or clone the camera and apply the new model while preserving the configured original. Unconfigured cameras change without a warning.
- Added an interactive scene Navigation Cube with six orthographic face targets, smooth transitions, drag-to-orbit synchronization, and a visibility preference.
- Added an opposite-corner fixed-size Scale Indicator with dynamic engineering units and a visibility preference.
- Scene Tree and Inspector restored-state arrow labels use stable Unicode escapes.

Static audit, JavaScript syntax, full Python regression suite, runtime preference/schema/UI/PTZ tests, and cache-957 guards pass. The managed local browser connection was unavailable in this Windows workspace, so owner visual interaction QA remains required after deployment.
Deployment passed on CCTV PID 4182721 at port 5010 with health status ok and exact frontend hashes. Server rollback: deploy_backups/pre_cache_957_20260727. Only the verified CCTV process was restarted; independent services and containers were untouched.
