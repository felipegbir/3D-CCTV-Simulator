# N.O.M.A.D. 8e.5 - Viewport Renderer Parity and Preference Polish

Status: complete; owner accepted 2026-07-24  
Scope authority: approved roadmap item 8e.5 plus owner-reported Preferences collapse correction

## Changes

- Dark is the default theme for new browsers and Reset Preferences.
- Existing explicitly saved theme choices remain respected.
- The Preferences group now collapses its preference rows as well as its Reset button.
- Camera viewport renderers inherit the main renderer's sRGB output color space, ACES Filmic tone mapping, exposure, shadow-map type, and quality-dependent shadow setting.
- Viewport renderers use the same high-performance WebGL preference and the preference-controlled pixel-ratio caps.

## Unchanged

- Project schema remains version 2.
- Camera hierarchy, PTZ restoration, project warnings, and browser-local preferences remain intact.
- Camera preset/thermography requirements and final comprehensive persistence reconciliation remain deferred to their recorded roadmap work.

## Automated validation

- Static audit passed with no missing/duplicate IDs or missing markers.
- All 8e.1-8e.4 regression and runtime tests passed.
- New 8e.5 dark-default, collapse, and renderer-parity guard passed.
- JavaScript module syntax and Python syntax checks passed.

## Deployment evidence

- Implementation commit: `9fad210`.
- Deployed to `192.168.2.208:/home/vmuser/nomad-cctv-simulator`.
- Accepted 8e.4 files preserved as `static/viewer.js.pre_8e5` and `templates/index.html.pre_8e5`.
- Restart targeted only the identity-verified CCTV process on port 5010.
- Process changed from PID `728025` to PID `746881`.
- `/api/health` returned healthy and `/api/cameras` returned 221 records.
- Deployed `viewer.js` SHA-256: `46c9656c4424ab0642c1c84ebc47a83526d2f36266785c9f3d6e3c0b9e38d6c4`.
- Deployed `index.html` file SHA-256: `953c3d711d3438c77a278c8cd5903f1f21d3b248d2b724deb875e8ff76b746f1`.
- No Docker command, container restart, or unrelated process signal was used.

## Owner acceptance

The owner confirmed that all 8e.5 functionality works, including Preferences collapse, theme defaults/persistence, and viewport behavior.

## Acceptance checklist

1. Hard-refresh `http://192.168.2.208:5010/?v=8e.5`.
2. Expand and collapse Operations > Preferences; confirm every preference row and Reset Preferences hide and return.
3. Select Reset Preferences and refresh; confirm Dark remains the default.
4. Explicitly select Light and refresh; confirm the saved Light choice remains respected.
5. Open one or more camera viewports and compare their color/exposure with the main scene.
6. Exercise Performance, Balanced, and High quality with camera viewports open; confirm stable rendering and acceptable performance.
7. Confirm viewport maximize, minimize, PTZ, palette, and capture still work.