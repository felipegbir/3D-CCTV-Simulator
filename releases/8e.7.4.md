# N.O.M.A.D. CCTV Simulator 8e.7.4

## PTZ preset recall correction

- Preserves the prior preset-list selection when reopening the Presets panel.
- Falls back deterministically to the active preset or first saved preset, so Recall never silently does nothing while presets exist.
- Displays an explicit message when no presets are available.
- Uses a normalized snapshot for each in-flight recall animation, protecting the target from concurrent metadata/UI refreshes.
- Retains the existing PTZ hierarchy, animated movement, optical/depth restoration, palette restoration, and post-animation ROI lifecycle.

## Verification

Static HTML/JavaScript audit, the full Python suite, all JavaScript runtime modules, module syntax validation, and backend compilation pass. The PTZ runtime regression executes scheduling and completion of a real Recall target and verifies pan, tilt, roll, zoom, HFOV, depth, root position, palette, active preset, and ROI hide/restore behavior.
