# N.O.M.A.D. 8e.1 - PTZ Save/Load Restoration

Status: **accepted and complete**  
Scope authority: approved roadmap item 8e.1 only

## Change

- Project loading now restores `pan`, `tilt`, `roll`, and `zoom` from the current nested camera data structure.
- Legacy project files with top-level PTZ fields remain supported.
- `applyCameraPtzRig(item)` now runs after camera data restoration and before projection refresh, ensuring the saved pivot orientation becomes visible immediately.
- Added `scripts/test_ptz_persistence.py` as a regression guard for persistence fields, save coverage, and call ordering.

## Unchanged

- PTZ hierarchy remains camera root > pan pivot > tilt pivot > roll pivot.
- Projection cone and render camera remain under the roll pivot.
- Project schema is unchanged; schema versioning belongs to 8e.2.
- No theme, preferences, presets, measurement, occlusion, persistence, reporting, or service-management work is included.

## Deployment evidence

- Deployed to `192.168.2.208:/home/vmuser/nomad-cctv-simulator/static/viewer.js`.
- Prior 8e viewer retained remotely as `static/viewer.js.pre_8e1`.
- Restart targeted only the verified CCTV process on port 5010.
- Process changed from PID `9994` to PID `628238`.
- `/api/health` returned healthy after restart.
- `/api/cameras` returned 221 records.
- HTTP-served `viewer.js` SHA-256: `ca9b4427566c78667c6e7c987b574476b85919c4e5b47f1e345aca76cb63211a`.
- Root page served successfully.
- No Docker command, container restart, or unrelated process signal was used.

## Validation results

- Static frontend contract audit.
- JavaScript module syntax check.
- PTZ persistence regression guard.
- Save/load round trip with non-zero pan, tilt, roll, and zoom.
- Projection cone and camera viewport remain aligned after load.

## Owner visual acceptance

1. Hard-refresh `http://192.168.2.208:5010/?v=8e.1`.
2. Add or select a camera and set non-zero pan, tilt, roll, and zoom.
3. Save the project JSON.
4. Change the PTZ values, then load the saved JSON.
5. Confirm inspector values, projection cone direction, and camera viewport return to the saved state.

Owner acceptance completed on 2026-07-24: loading the saved project restored cameras, assigned camera models, and PTZ settings. 8e.2 is authorized.

## Owner acceptance result

**Passed on 2026-07-24.** The project owner confirmed that loading the saved file restored cameras with their selected camera models and PTZ settings.
